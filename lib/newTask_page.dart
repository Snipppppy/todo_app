
import 'package:flutter/material.dart';

class NewTasksPage extends StatelessWidget {
  const NewTasksPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return(const Scaffold(
      body: Text('NewTask from a Seperate page file.'),
    ));
  }
}